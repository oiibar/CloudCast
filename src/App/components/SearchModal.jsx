import React from "react";
import Search from "../../sections/Main/Search";

const SearchModal = ({ onClose }) => {
  return <Search onClose={onClose} onSearch={() => {}} />;
};

export default SearchModal;
